# Higs

Basic proxy client and server pair.

Client works as a transparent proxy client on local machine. It can filter outgoing traffic based on packet destination address
to either forward it to server or send directly to destination.
